import React from "react";
import { Transition as SUIRTransition } from "@infinitecsolutions/semantic-ui-react";

const Transition = props => {
  return <SUIRTransition {...props} />;
};

export default Transition;
